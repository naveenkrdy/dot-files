# RTK - Rust Token Killer

All bash commands are auto-proxied via the PreToolUse hook - no action needed.

Meta commands (run directly): `rtk gain` (savings analytics), `rtk gain --history`, `rtk discover` (missed opportunities), `rtk proxy <cmd>` (bypass filter for debugging).
